# Base URL for Vercel API routes (no trailing slash).
# Expo: set this so the app calls production/staging APIs.
# Web (CRA): leave unset or empty to use same-origin `/api/...`.
EXPO_PUBLIC_API_BASE_URL=https://doodlestories.app
